# DECISIONS.md

記錄專案裡卡住需要使用者決定的關鍵時刻：問題是什麼、有哪些選項、每個選項的
後果、最後選了什麼、為什麼。目的是讓之後回來看的人（包括未來的你）不用翻
聊天紀錄就能看懂為什麼專案長這樣。新的一筆加在最上面（新到舊）。

---

## 2026-08-22 — Task 5：ML 因子合成完成，margin_usage 在非線性方法下依然沒有訊號

**狀態：不是卡住待決定，是主動記錄 Task 5 的完整結果，供你之後評估**

**背景**：Task 2 用線性 IC 排查後把 `inst_flow`／`margin_usage` 移出主策略
複合（見上方「inst_flow／margin_usage IC 偏低」那筆），移交 Task 5 用
非線性方法（LightGBM）重新評估。Task 5 建立 `strategy/ml_composite.py`：
特徵＝五個核心因子（momentum/value/rev_yoy/low_vol/margin_usage，
inst_flow 延續 Task 2 的決定不重新加回來）＋機制 one-hot（BULL/NEUTRAL/
WARNING/BEAR）＋大盤波動分位，標籤＝個股未來 20 日報酬橫截面分位數，
LightGBM 迴歸（n_estimators 500／max_depth 5／learning_rate 0.01／
early stopping 50），walk-forward 每年 1 月 1 日重訓一次，訓練資料是
該年以前的「全部」歷史，且用全市場所有股票（不限流動性前 300、不限
最終選中的 30 檔）——刻意跟 Task 2 的檢驗方法（流動性前 300 檔＋線性
排名 IC）完全不同，才是真正獨立的第二次檢驗。

**結果（真實訓練，9 個 walk-forward fold，2018-2026，訓練樣本 19.7 萬～
59.2 萬筆不等）**：

逐年 feature importance（`reports/feature_importance_by_year.md`）與
因子穩定性分析（`reports/factor_stability_analysis.md`）顯示：
- **momentum、low_vol 持續是最重要的兩個因子**（9 年中 low_vol 有 5 年
  排名第一，momentum 排名穩定在 1-3 之間，排名標準差分別是 0.73、0.60）
- **margin_usage 連續 9 年都是 5 個因子裡重要性最低**（排名標準差 = 0，
  代表這不是「有時候有用有時候沒用」的雜訊型因子，而是穩定地不重要）——
  跟 Task 2 線性 IC 排查的結論一致（IC -0.0039，接近全部因子裡最弱），
  這次用完全不同的方法（非線性模型＋全市場樣本）重新驗證，結論相同：
  **margin_usage 不是「線性方法測不出來的隱藏訊號」，是真的沒有訊號**。
- rev_yoy 排名標準差最高（0.93，5 個因子裡最不穩定），某些年重要性
  接近 momentum，某些年接近 margin_usage，看起來比較看市況。
- value 排名相對穩定但重要性長期偏低（多數年份排名第 4）。

**SHAP summary**（`reports/shap_summary.png`，用最後一個 fold［2026 年
測試集］抽樣 3000 筆畫出）與逐年 gain-based importance 兩者結論一致，
互相印證不是單一方法的偏差。

**端到端回測比較**（2015-01-05～2026-04-17，ML 版的預測分數只從
2018 年才開始有值，2015-2017 是 walk-forward 暖機期，這段期間 ML 版
等於空手／低配置——這不是公平的逐年對照，但可以看出整體可用性）：

| 指標 | 基準版（Task 2，動態 IC 加權） | ML 版（Task 5） |
|---|---|---|
| 總報酬 | 92.1% | 64.3%（含 2015-17 空手期的拖累） |
| 年化報酬 | 6.5% | 6.8% |
| Sharpe | 0.39 | 0.44 |
| 最大回撤 | -24.9% | -34.0% |
| 年化換手率 | 255.2% | 330.2% |

年化報酬與 Sharpe 都比基準版略好，但總報酬較低（主要是 2015-2017
沒有 ML 預測值可用，等於錯過那三年的報酬機會，拉低總報酬——用年化
數字對照比較公平）、回撤更大、換手率也更高。這不是「ML 版明顯更好」
或「明顯更差」的結果，是好壞參半，**不建議現在就把 `use_ml_composite`
設成預設值**，維持 Task 2 線性版為預設（`use_ml_composite: bool = False`），
ML 版本作為可選路徑保留，供 Task 6 的 ablation（D：ML＋機制曝險）
與之後的策略選擇參考。

**額外要求：訓練策略比較（只用最近一年 vs 最近三年加權平均）**——
簡化版 walk-forward（2023-2026 共 4 年，n_estimators 縮小到 150 控制
運算時間，兩種策略同一設定公平比較，用逐日橫截面 IC 當指標）：
最近三年加權平均（0.5/0.3/0.2）平均 IC 較高（0.0629 vs 0.0532），但
年度間標準差沒有比較小（0.0247 vs 0.0220）——是報酬與穩定性的取捨，
不是單方面完勝，這個結果只放進 `reports/factor_stability_analysis.md`
供之後參考，**不會現在就拿來改主模型的訓練方式**（主模型維持任務書
規格的 expanding window／全部歷史）。

**後果**：
- 新增 `strategy/ml_composite.py`；`strategy/quant_layer2.py` 的
  `build_positions()`／`run_pipeline()` 新增 `ml_scores`／
  `use_ml_composite` 參數（預設 `False`，跟 `use_regime_weights`
  互斥，同時開啟會丟 `ValueError`——兩者結合的明確語意留給 Task 6
  的 ablation D 再定義，不在 Task 5 範圍內）
- `reports/shap_summary.png`、`reports/feature_importance_by_year.md`、
  `reports/factor_stability_analysis.md` 三份報告全部是真實訓練結果，
  無估計值
- `tests/test_ml_composite.py` 新增 10 項測試（合成資料），pytest
  全部 51 項通過（41 舊＋10 新）
- `requirements1.txt` 補上 `shap>=0.44.0`

---

## 2026-08-22 — Task 4：機制動態版績效遠低於基準版，需要你知道這件事

**狀態：不是卡住待決定，是主動記錄一個重要發現，供你評估**

**問題**：Task 4 把 Task 3 的機制標籤接進策略後，機制動態版（equal
weighting）的回測結果是：總報酬 13.6%、年化報酬 1.2%、Sharpe -0.11；
risk_parity 版類似（12.9%／1.2%／-0.14）。同一段期間（2015-01～2026-04）
的基準版（Task 2 的動態 IC 權重、沒有機制擇時）是：總報酬 92.1%、
年化報酬 6.5%、Sharpe 0.39。機制動態版的績效遠遠不如基準版。

**根因（延續 Task 3 已經記錄過的發現，不是新 bug）**：Task 3 的
`docs/DECISIONS.md`／`reports/health_score_breadth_divergence.md` 已經
記錄過：BULL 狀態在 2012-2026 全期只出現在 1.9% 的交易日，系統絕大多數
時間判定為 WARNING（曝險 0.4）或 BEAR（曝險 0.1）。這段回測期間台股
剛好是史詩級的大多頭（2015 年 TAIEX 約 9000 點漲到 2026 年約 47000 點），
機制系統因為太保守，幾乎全程只用 10-40% 的曝險參與這段大多頭，錯過了
大部分漲幅——這是 Task 3 已知特性的直接後果，不是 Task 4 整合過程中的
新錯誤（機制動態版跑通了、risk_parity 換手 ≤ equal 版 × 1.2 也驗證通過，
見下方驗收結果）。

**這裡不是要你現在做決定**，只是主動標記：如果之後要拿這個機制系統做
實際的曝險控制，目前的校準方式（六個廣度型健康分數成分 + 18天升級/3天
降級的遲滯機制）在牛市中會嚴重犧牲報酬，換來的是更低的波動度與回撤
（年化波動度 2.4-2.6% vs 基準版 12.6%，回撤 -7.6~-8.2% vs -24.9%）——
這是一個風險/報酬的取捨，不是免費的午餐。要不要調整機制的校準方式讓
它在大多頭時期更願意加碼、什麼時候要做這個調整，等你之後決定，不影響
Task 4 本身的驗收（見下方）。

**Task 4 驗收結果**：
- 機制動態版跑通：✅（equal 與 risk_parity 兩種 weighting 都能跑完整個
  流程，無錯誤）
- risk_parity 換手 ≤ equal 版 × 1.2：✅（64.4% ≤ 64.9% × 1.2 = 77.9%）
- 換手分解報告產出：✅ `reports/turnover_decomposition.md`（換股
  30.1%／權重調整39.8%／擇時貢獻 30.1%）

**與任務書規格的一個已知偏離**：任務書原始的 `REGIME_FACTOR_WEIGHTS`
含 5 個因子（含 inst_flow，例如 BULL 給 inst_flow 0.30 權重）。延續
2026-08-18 Task 2 的決定（`inst_flow` 經四輪真實資料 IC 排查後判定為
誠實的負面結果，移出主策略複合），Task 4 的 `REGIME_FACTOR_WEIGHTS`
把 inst_flow 拿掉、剩餘四個因子依原始任務書給的相對比例重新正規化
（例如 BULL 原本 momentum:.30/value:.10/rev_yoy:.25/low_vol:.05/
inst_flow:.30，四項總和 .70，各自除以 .70）。這不是新的判斷，是延續
已經記錄、已經有理由的舊決定，不需要重新走一次決策流程，但在這裡註記
一筆，避免之後對照任務書原文時看到數字不一致而困惑。完整權重表跟理由
在 `strategy/quant_layer2.py` 的 `REGIME_FACTOR_WEIGHTS` 常數註解裡。

---

## 2026-08-18～2026-08-21 — Task 3 機制偵測：3 項驗收標準未通過，怎麼處理

**狀態：已關閉**（資料範圍已延伸並驗證；2016-17／2023-24 兩項比例最終
判定為系統設計的誠實特性，接受現狀，不繼續調參數硬湊）

**問題**：Task 3（機制偵測模組）7 項驗收標準中，最初只有 4 項用真實資料
驗證通過，3 項沒有通過（切換次數、2016-17 BULL 比例、2023-24 BULL 比例）。

**最終驗收結果**（2026-08-21，資料延伸到 2012-05-02 並套用 hysteresis
調整後，唯一一次完整驗收，未分輪重複測試）：

| 驗收項目 | 標準 | 最初結果（2015 起） | 最終結果（2012-05-02 起） | 通過？ |
|---|---|---|---|---|
| 2020/02-03 WARNING/BEAR | ≥15 天 | 41/41 天 | 41/41 天 | ✅ |
| 2022 WARNING+BEAR | >80 天 | 230 天 | 246 天 | ✅ |
| 2026/04、2026/07 大跌標記 | WARNING/BEAR | 兩段皆全部標記 | 兩段皆全部標記 | ✅ |
| 2026/07 底 squeeze_ratio | 有值且 <1.0 | 0.83-0.96 | 0.83-0.96（未變） | ✅ |
| **全期機制切換次數** | **<40 次** | **63 次** | **33 次** | ✅ |
| **2016-17 多頭 BULL 比例** | **>60%** | **0%（完全無資料）** | **0%（490 天全部有真實資料，但仍是 0）** | ❌（見下方新發現） |
| **2023-24 多頭 BULL 比例** | **>60%** | **18.9%** | **5.2%** | ❌ |
| HMM 無 look-ahead（tests/test_regime.py） | 斷言通過 | 通過 | 通過 | ✅ |

**切換次數怎麼解決的**——把 63 次切換拆解，發現只有 9 次（14%）是單純的
BULL↔NEUTRAL 邊界抖動，其餘 54 次都牽涉 WARNING 或 BEAR，多半是真實市況
風險變化。改採不對稱調整：只拉長「升級」確認天數（10→18 天），不動
「降級」天數（維持 3 天）。切換次數 63→33（通過 <40），2020/02、2022
兩次真實危機的偵測速度完全沒有變慢（降級判斷沒改）。完整分析見
`regime/regime_engine.py` 的 UPGRADE_DAYS 常數註解。

**資料延伸執行結果**——2026-08-21 完成 institutional_investors／
margin_trading／market_index 三張表往回補到 2012-05-02（單一 process、
沿用鎖檔+額度自動休眠機制，過程中觸發 5 次配額耗盡休眠，全部自動恢復，
零失敗）：institutional_investors 新增 229.5 萬筆（2056 檔股票，最早日期
2012-05-02）、margin_trading 新增 85.8 萬筆（1957 檔股票，最早日期
2012-05-02）、market_index 新增 666 筆。**2016-17 從此不再是 NaN 空窗**
（HMM 504 天門檻現在落在 2014 年中，2016-2017 兩年共 490 個交易日全部
有真實的健康分數／p_bear／crash_prob 輸出）。

**重要新發現：2016-17 BULL 比例依然是 0%，資料延伸沒有解決這個問題**——
這跟原本的預期不同，原本以為「資料延伸=解決」，但實測發現：2016-17
兩年裡，「health≥60 且 p_bear<0.3 且 crash_prob<0.3」這個 BULL 判定
條件本身其實有 20.6% 的天數符合，但**最長連續滿足天數只有 17 天**，
剛好比新的 18 天升級門檻少 1 天，所以整整兩年從來沒有真正確認過一次
BULL。用資料延伸前的舊門檻（10 天升級）回測同一段延伸後的資料，2016-17
BULL 比例會回升到 7.1%，但仍然遠低於 60%，而且會讓全期切換次數回升到
73 次（不通過 <40 的標準）。這代表 2016-17 的低 BULL 比例**不是資料
覆蓋問題（已經徹底解決），而是健康分數本身在這兩年很少連續達到「非常
健康」的門檻**——攤開全期資料看，BULL 狀態在 2012-2026 整整 14 年裡只
出現在 1.9% 的交易日（其餘 57.8% WARNING、35.0% BEAR、5.3% NEUTRAL），
是全期都很罕見的狀態，不是 2016-17／2023-24 特別異常。

**根因判定（兩項比例偏低的共同結論）**：健康分數的六個成分（波動、
寬度、高低差、相關性、不對稱性、squeeze）全部是市場廣度／微結構型
指標，加上遲滯機制刻意設計成「快降慢升」，兩者疊加的結果是 BULL
這個最高等級的狀態被設計得很難確認——這是誠實的系統行為，不是 bug，
但 BULL 出現頻率遠低於任務書驗收標準原本設想的比例。

**選項**：
A. 接受現狀：兩項比例維持「誠實地沒通過」，在 Task 3 報告如實記錄，
   進 Task 4；BULL 狀態罕見這件事本身也是有用的研究發現（機制偵測
   系統對「真的很健康」的認定門檻很高，不會輕易給出滿倉建議）
B. 進一步調鬆 hysteresis（例如把升級天數從 18 調回 15 或更低），换取
   BULL 比例回升，但會讓切換次數重新超過 40（已實測 up15/down3：切換
   44 次、2016-17 6.1%、2023-24 8.9%，兩個標準只能二選一，無法同時滿足）
C. 重新設計健康分數的門檻或成分（例如降低 BULL 判定的 health 門檻，
   從 60 降到接近實際分佈中位數），讓 BULL 出現頻率貼近任務書預期，
   但這已經是直接調整分類標準去湊比例，屬於本專案一直避免的「調參數
   硬湊結果」

**選擇**：選項 A。

**為什麼**：B 選項證明了 40 次切換與 60% BULL 比例這兩個驗收標準在目前
的系統設計下互斥（switch count 越低，代表遲滯期越長，BULL 越難確認），
魚與熊掌不可兼得，沒有一個「正確」的中間值——選哪個都是主觀取捨，
不是排查能解決的技術問題。C 選項是本專案從一開始就明確拒絕的做法
（Task 2 的 inst_flow／margin_usage 已經示範過一次「發現問題就調整
定義去湊過關」是不該做的事）。切換次數（<40，已通過）比 BULL 比例
（>60%，此次判定為系統誠實特性）更貼近「這個系統能不能實際拿來用」
的工程需求，所以以切換次數的通過為優先，BULL 比例維持誠實的未通過
紀錄。

**後果**：
- `regime/regime_engine.py`：`run_regime_engine()` 的 `start` 預設值改成
  `"2012-05-02"`，`UPGRADE_DAYS` 維持 18（`DOWNGRADE_DAYS` 維持 3）
- `regime/ml_alert.py`：`FIRST_TEST_YEAR` 從 2016 調整到 2014（配合資料
  延伸後 2012+2013 合併有 24 筆崩盤標籤，足夠訓練門檻）
- 最終驗收：6/8 項通過（原本 4/8，切換次數新增通過，2016-17／2023-24
  兩項確認為誠實的系統特性、不是資料或程式問題）
- `docs/DATA_AVAILABILITY.md` 補充存活者偏誤曝險時間拉長的正式記錄

---

## 2026-08-17 — inst_flow／margin_usage IC 偏低，複合權重怎麼處理

**狀態：已決定（2026-08-18）— 選項 C**

**問題**：`inst_flow`（IC 0.0034）與 `margin_usage`（IC -0.0039）兩個因子的
20 日前瞻 Spearman IC 都遠低於 0.03 的驗收門檻。

**排查過程（四輪，全部完成，結論一致：不是 bug，是真的沒有訊號）**：

1. **正規化前後對照**（全樣本，無流動性篩選）：-0.0003 vs -0.0024，都偏弱，
   排除「正規化本身造成 IC 偏低」
2. **資料補齊前後對照**：margin_usage 用完整回填後的真實資料（1957 檔）重算，
   仍是 -0.0039，排除「資料不足造成 IC 偏低」
3. **SQL 逐筆核對真實 API**：抽查 5 檔股票（2330/2317/1101/2882/3008）×
   5 個橫跨 2016-2025 的日期，`institutional_investors` 與 `margin_trading`
   兩張表共 50 筆資料，跟即時重打的 FinMind API **全部逐筆一致（50/50）**，
   排除「資料存錯」
4. **短窗口前瞻報酬**：換 5 日／10 日重測，兩個因子在更短窗口下的 IC **都比
   20 日更差**（inst_flow 5日 -0.0013、10日 -0.0015、20日 0.0034；margin_usage
   5日 -0.0065、10日 -0.0081、20日 -0.0039），20 日窗口已經是三者中最好的，
   不是窗口選錯
5. **分年份 IC**（2015-2026）：兩個因子逐年 IC 在正負之間跳動，沒有「某幾年
   訊號很強、被其他年份拉平」的模式——inst_flow 2022 年明顯偏差（-0.0342），
   margin_usage 2019/2023/2024/2025 都是負的、2018/2020-2022 是正的，看起來
   像隨機雜訊，不像被稀釋掉的真訊號（2026 兩者都轉正但樣本只有 46 個交易日，
   數字不可靠，不當作證據）

**結論**：這是誠實的負面結果，不是 bug。兩個因子用目前的定義
（60 日累積買超金額、20 日融資變化率），對未來 5/10/20 日報酬沒有穩定的
線性/單調預測力。

**選項（供使用者選擇，見對話中的白話說明）**：
A. 維持任務書規格的複合權重（inst_flow 30%）
B. 大幅調降 inst_flow 的複合權重，margin_usage 維持排除在外（現狀已排除）
C. 把 inst_flow 也移出主策略複合，兩者都留給 Task 5 的 ML 版本重新評估非線性關係
D. 維持現有的動態 IC 加權機制不變（已經會自動調降低 IC 因子的權重），只調整
   fallback 預設值

**選擇**：選項 C——`inst_flow` 移出主策略複合權重，程式碼保留（不刪除），
與 `margin_usage` 一起留給 Task 5 的 LightGBM 版本重新評估非線性/條件式訊號。

**為什麼**：
1. 訊號在多個獨立角度的檢驗下一致缺席——不是單一測試偶然沒過，而是正規化
   方式、資料完整性、原始資料正確性、短窗口重測、分年份拆解五個互相獨立的
   角度都指向同一個結論，判斷這是真的沒有線性可用的訊號，不是測試方法選錯。
2. 線性排名（cross-sectional IC）方法本身有侷限性，只能抓到單調關係，
   抓不到「連續買超才有效」這類條件式或非線性模式。與其武斷地給一個已知
   在目前方法下無效的訊號分配權重（選項 A/B 都是），不如誠實移出，移交給
   更適合捕捉這類模式的方法（Task 5 的 ML 模型）。
3. 選項 D 的風險是動態加權機制用 IC 絕對值計算權重，雜訊擺動的絕對值不會
   真的趨近於零，等於系統可能還是會給一個純雜訊訊號一些權重，沒有真正
   解決問題。

**後果**：
- `strategy/quant_layer2.py`：`build_positions()` 的 `factor_map`／
  `default_weights` 移除 `inst_flow`；剩餘四因子（momentum/value/rev_yoy/
  low_vol）依實測 20 日 IC 均值相對強弱重新正規化：momentum 0.34 / value
  0.18 / rev_yoy 0.18 / low_vol 0.30（原始 IC：0.038/0.020/0.020/0.033）。
  `build_factors()` 仍計算並回傳 `inst_flow`、`margin_usage`，只是不再進
  複合分數。
- 新增 `reports/factor_negative_findings.md`：正式的負面研究發現報告，
  記錄理論假設、四輪排查方法與結果、結論、後續規劃，準備納入 README 的
  Limitations 章節。
- 移除 inst_flow 前後的策略績效對比、pytest 回歸結果，記錄在對應的 Task
  交付報告中（規則 9 白話格式）。

---

## 2026-08-17 — 402 配額誤判為「無資料」的 bug

**問題**：margin_trading 全量回填實際上只真正抓到 91/2095 檔（約 4%），但執行
統計顯示「成功 96、失敗 0、跳過 1284」，表面上看起來像是正常的批次跳過，完全
看不出實際上發生了什麼。

**根因**：`_fetch()` 裡 `resp.raise_for_status()` 在檢查 FinMind 回傳的 JSON
`status` 欄位之前，就先對真正的 HTTP 402 狀態碼拋出 `HTTPError`——導致原本設計
的「偵測到 402 就等待配額恢復」那段邏輯從來沒有被執行到，是死碼。402 實際上
被 generic 的 HTTPError 分支接住，只做幾秒鐘的指數退避重試，3 次都失敗就放棄，
把空 DataFrame 回傳給上層，被誤判成「FinMind 確認無資料」，計入「正常跳過」。
`download_institutional.py` 有完全一樣的 bug。

背後還有一個結構性成因：完全沒有機制阻止多個下載 process 同時使用同一個
`FINMIND_TOKEN`——這個帳號等級的配額上限是 600 次/小時，但兩支下載器的限速器
都只在自己的 process 記憶體裡計數，互相看不到對方，兩個 process 同時跑會一起
把共用配額用光。這個問題在診斷過程中實際發生了兩次。

**選項**：
1. **只修 402 判斷邏輯，不加互斥鎖**——治標不治本，下次還是可能有人（或自動化
   排程）不小心同時啟動兩個下載程序，配額衝突會重演
2. **互斥鎖 + 修正 402 判斷 + 402 改成休眠等待自動恢復（不再重試）**——完整修復
   兩個根因
3. **放棄背景自動回填，改成每次都要有人盯著手動跑**——不符合「無人值守長時間
   背景執行」的實際需求，且問題根本沒解決，只是靠人力補償

**選擇**：方案 2（完整修復）。

**為什麼**：根因診斷顯示這是兩個獨立問題疊加（判斷邏輯 bug + 缺乏並發保護），
只修其中一個，另一個下次還是會造成同樣的災情。使用者明確要求要能無人值守
背景執行數小時，方案 1 和 3 都不滿足這個需求。

**後果**：
- 新增 `data_pipeline/download_lock.py`（PID 互斥鎖）、`data_pipeline/finmind_common.py`
  （兩支下載器共用的配額例外類別、心跳、circuit breaker 常數）
- 兩支下載器的順序模式（`workers=1`，也是目前唯一實際使用的模式）現在有完整的
  「402 → 休眠到下個整點 → 自動恢復 → 從 checkpoint 繼續」機制，連續 3 個週期
  仍失敗才放棄
- `download_institutional.py` 的並行模式（`workers>1`）只做了正確性修復（402
  不再被誤判成 skip）+ 簡易 circuit breaker，**沒有**完整跨執行緒的休眠協調——
  已知限制，記錄在 `docs/PROJECT_STATUS.md`，需要無人值守長跑請用 `workers=1`
- 重新跑過一輪完整 backfill 後，Task 1 的 institutional／margin_trading／TAIEX
  三項驗收全部通過
